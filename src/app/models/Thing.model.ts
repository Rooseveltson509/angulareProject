import { Subscription } from "rxjs";

export class Thing {

  _id!: string;
  title!: string;
  description!: string;
  imageUrl!: string;
  price!: number;
  userId!: string;

/*   public static of(res: Subscription): Thing {
    let out: Thing = new Thing();
     return out;
    } */



}
